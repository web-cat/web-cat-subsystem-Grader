---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/org/webcat/grader/Submission.java]
autonomous: true
requirements: [CORE-01, CORE-02]

must_haves:
  truths:
    - "StudentSubmissionInfo exists as a static inner class in Submission.java"
    - "StudentSubmissionInfo tracks User, AssignmentOffering, gradedSubmission, and allSubmissions (NSArray<Submission>)"
    - "SubmissionGradingState exists as a static inner class in Submission.java"
    - "SubmissionGradingState subclasses NSMutableDictionary<AssignmentOffering, Map<User, StudentSubmissionInfo>>"
    - "SubmissionGradingState has fields and public getters for lastFetchTimestamp (NSTimestamp), offerings (NSArray<AssignmentOffering>), and users (NSArray<User>)"
  artifacts:
    - path: "src/org/webcat/grader/Submission.java"
      provides: "Core data structures for submission fetching"
      contains: ["StudentSubmissionInfo", "SubmissionGradingState"]
  key_links:
    - from: "SubmissionGradingState"
      to: "NSMutableDictionary"
      via: "inheritance"
      pattern: "public static class SubmissionGradingState extends NSMutableDictionary"
---

<objective>
Implement core data structures `StudentSubmissionInfo` and `SubmissionGradingState` as static inner classes in `Submission.java`.

Purpose: Provide foundational storage and tracking for student submissions to enable batch fetching in later phases.
Output: Updated `Submission.java` with two new static inner classes and required import.
</objective>

<execution_context>
@/Users/edwards/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/edwards/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/org/webcat/grader/Submission.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement StudentSubmissionInfo</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Add `import java.util.Map;` to `Submission.java`.
    - Near the `CumulativeStats` inner class (approx line 2461), implement `public static class StudentSubmissionInfo`.
    - Fields for `StudentSubmissionInfo`:
        - `public User user;`
        - `public AssignmentOffering offering;`
        - `public Submission gradedSubmission;`
        - `public NSArray<Submission> allSubmissions;`
    - Include a simple constructor to initialize these fields.
  </action>
  <verify>
    - Grep for `StudentSubmissionInfo` in `Submission.java`.
    - Check for `import java.util.Map;` at top.
  </verify>
  <done>StudentSubmissionInfo class is implemented and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SubmissionGradingState</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Near `StudentSubmissionInfo`, implement `public static class SubmissionGradingState`.
    - Inheritance: `extends NSMutableDictionary<AssignmentOffering, Map<User, StudentSubmissionInfo>>`.
    - Fields:
        - `public NSTimestamp lastFetchTimestamp;`
        - `public NSArray<AssignmentOffering> offerings;`
        - `public NSArray<User> users;`
    - Implementation: 
        - Provide a constructor that initializes the dictionary and these fields.
        - Ensure public getters exist for these fields to follow KVC patterns: `lastFetchTimestamp()`, `offerings()`, `users()`.
  </action>
  <verify>
    - Grep for `SubmissionGradingState` in `Submission.java`.
    - Verify inheritance from `NSMutableDictionary`.
    - Check for public getters.
  </verify>
  <done>SubmissionGradingState class is implemented with KVC-compatible accessors.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Compilation</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Attempt to compile the project to ensure no syntax errors or missing imports.
    - Since this is a framework, `ant compile` or similar command from `build.xml` should be used.
  </action>
  <verify>
    <automated>ant compile</automated>
  </verify>
  <done>Project compiles successfully with new classes.</done>
</task>

</tasks>

<verification>
Check that the new inner classes are present and follow the specified structures.
</verification>

<success_criteria>
1. `Submission.java` compiles without error.
2. `StudentSubmissionInfo` correctly references the required types.
3. `SubmissionGradingState` correctly extends `NSMutableDictionary` and provides KVC accessors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
