---
phase: 04-ui-compatibility
plan: 01
type: execute
wave: 1
depends_on: [03-01]
files_modified: [src/org/webcat/grader/UserSubmissionPair.java, src/org/webcat/grader/tests/UserSubmissionPairTest.java]
autonomous: true
requirements: [COMPAT-01]

must_haves:
  truths:
    - "UserSubmissionPair.fromInfoMap converts Map to NSArray correctly"
    - "Resulting NSArray preserves order of optional users list"
    - "Partner links are filtered when omitPartners is true"
    - "Statistics are accumulated in CumulativeStats during conversion"
  artifacts:
    - path: "src/org/webcat/grader/UserSubmissionPair.java"
      provides: "Bridge methods for UI compatibility"
    - path: "src/org/webcat/grader/tests/UserSubmissionPairTest.java"
      provides: "Unit tests for the bridge logic"
  key_links:
    - from: "UserSubmissionPair.fromInfoMap"
      to: "Submission.StudentSubmissionInfo"
      via: "Iteration and extraction of gradedSubmission"
    - from: "UserSubmissionPair.fromInfoMap"
      to: "Submission.CumulativeStats"
      via: "Call to accumulate(sub)"
---

<objective>
Implement bridge methods in UserSubmissionPair to maintain UI compatibility with the new batch-fetching infrastructure.

Purpose: Allow existing UI components (like StudentsForAssignmentPage) to work with the new data structures (StudentSubmissionInfo maps) without extensive refactoring.
Output: fromInfoMap helper methods in UserSubmissionPair and corresponding unit tests.
</objective>

<execution_context>
@/Users/edwards/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/edwards/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-compatibility-layer/04-RESEARCH.md
@src/org/webcat/grader/UserSubmissionPair.java
@src/org/webcat/grader/Submission.java

<interfaces>
From src/org/webcat/grader/Submission.java:
```java
public static class StudentSubmissionInfo {
    public User user;
    public AssignmentOffering offering;
    public Submission gradedSubmission;
    public NSMutableArray<Submission> allSubmissions;
    public boolean isDirty;
}

public static class CumulativeStats {
    public CumulativeStats(String scoreKeyPath);
    public CumulativeStats();
    public void accumulate(Submission submission);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit tests for fromInfoMap bridge</name>
  <files>src/org/webcat/grader/tests/UserSubmissionPairTest.java</files>
  <behavior>
    - Test fromInfoMap with null input returns empty array.
    - Test fromInfoMap converts map to pairs correctly.
    - Test fromInfoMap with users list preserves order.
    - Test fromInfoMap filters partner links when omitPartners is true.
    - Test fromInfoMap handles users with no submissions (null gradedSubmission).
    - Test fromInfoMap calls accumulate on CumulativeStats.
  </behavior>
  <action>
    Create src/org/webcat/grader/tests directory if it doesn't exist.
    Implement UserSubmissionPairTest extending junit.framework.TestCase.
    Mock or use real User and Submission objects to verify the logic.
    Note: Since this is EOF, using real objects might require an editing context. Using simple mocks or stubbed EOF objects is preferred if possible.
  </action>
  <verify>
    <automated>ant test -Dtest=org.webcat.grader.tests.UserSubmissionPairTest</automated>
  </verify>
  <done>Failing tests exist for all required fromInfoMap overloads.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fromInfoMap bridge methods</name>
  <files>src/org/webcat/grader/UserSubmissionPair.java</files>
  <action>
    Add "import java.util.Map;" to imports.
    Implement two overloads of fromInfoMap as suggested in research:
    1. fromInfoMap(Map<User, Submission.StudentSubmissionInfo> infoMap, boolean omitPartners, Submission.CumulativeStats accumulator)
    2. fromInfoMap(Map<User, Submission.StudentSubmissionInfo> infoMap, NSArray<User> users, boolean omitPartners, Submission.CumulativeStats accumulator)

    Implementation details:
    - Iterate over infoMap values (overload 1) or users list (overload 2).
    - Extract gradedSubmission from StudentSubmissionInfo.
    - Skip if omitPartners is true and sub.partnerLink() is true.
    - Accumulate if accumulator is non-null and sub is non-null.
    - Add new UserSubmissionPair to result array.
  </action>
  <verify>
    <automated>ant test -Dtest=org.webcat.grader.tests.UserSubmissionPairTest</automated>
  </verify>
  <done>All overloads implemented and tests pass.</done>
</task>

</tasks>

<verification>
Verify that the new bridge methods correctly handle all cases defined in research, especially partner filtering and statistical accumulation.
</verification>

<success_criteria>
1. UserSubmissionPair.java contains both fromInfoMap overloads.
2. UserSubmissionPairTest.java passes all tests.
3. Bridge logic correctly handles nulls, order preservation, and partner filtering.
</success_criteria>

<output>
After completion, create .planning/phases/04-ui-compatibility/04-01-SUMMARY.md
</output>
