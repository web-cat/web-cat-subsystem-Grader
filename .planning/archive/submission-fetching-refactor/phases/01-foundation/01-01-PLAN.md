---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/org/webcat/grader/Submission.java]
autonomous: true
requirements: [CORE-01, CORE-02]

must_haves:
  truths:
    - "StudentSubmissionInfo exists as a static inner class in Submission.java"
    - "StudentSubmissionInfo tracks User, AssignmentOffering, gradedSubmission, allSubmissions (NSArray<Submission>), and isDirty (boolean)"
    - "SubmissionGradingState exists as a static inner class in Submission.java"
    - "SubmissionGradingState subclasses NSMutableDictionary<AssignmentOffering, Map<User, StudentSubmissionInfo>>"
    - "SubmissionGradingState provides two constructors: Initial (offerings, optional users) and Incremental (existing state)"
    - "SubmissionGradingState has helper methods: resultsForOffering(AssignmentOffering) and infoForUser(AssignmentOffering, User)"
    - "SubmissionGradingState has fields and public getters for lastFetchTimestamp (NSTimestamp), offerings (NSArray<AssignmentOffering>), and users (NSArray<User>)"
  artifacts:
    - path: "src/org/webcat/grader/Submission.java"
      provides: "Core data structures for submission fetching"
      contains: ["StudentSubmissionInfo", "SubmissionGradingState"]
  key_links:
    - from: "SubmissionGradingState"
      to: "NSMutableDictionary"
      via: "inheritance"
      pattern: "public static class SubmissionGradingState extends NSMutableDictionary"
---

<objective>
Implement core data structures `StudentSubmissionInfo` and `SubmissionGradingState` as static inner classes in `Submission.java` with constructors and helper methods for batch fetching.

Purpose: Provide foundational storage and tracking for student submissions to enable batch and incremental fetching in later phases.
Output: Updated `Submission.java` with two new static inner classes and required imports.
</objective>

<execution_context>
@/Users/Shared/Web-CAT/Grader/.planning/phases/01-foundation/01-01-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/org/webcat/grader/Submission.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add required imports</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Add `import java.util.Map;` and `import java.util.HashMap;` to `Submission.java` (around line 35).
  </action>
  <verify>
    - Check for `import java.util.Map;` and `import java.util.HashMap;` in `Submission.java`.
  </verify>
  <done>Required imports are present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement StudentSubmissionInfo</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Near the `CumulativeStats` inner class (approx line 2461), implement `public static class StudentSubmissionInfo`.
    - Fields (public):
        - `public User user;`
        - `public AssignmentOffering offering;`
        - `public Submission gradedSubmission;`
        - `public NSArray<Submission> allSubmissions;`
        - `public boolean isDirty;`
    - Constructors:
        - Include a constructor to initialize these fields.
    - KVC Getters:
        - Provide public getters for these fields (e.g., `user()`, `offering()`, `gradedSubmission()`, `allSubmissions()`, `isDirty()`).
  </action>
  <verify>
    - Grep for `StudentSubmissionInfo` in `Submission.java`.
    - Check for `isDirty` field.
    - Check for public getters.
  </verify>
  <done>StudentSubmissionInfo class is implemented with all required fields and KVC-friendly accessors.</done>
</task>

<task type="auto">
  <name>Task 3: Implement SubmissionGradingState</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Near `StudentSubmissionInfo`, implement `public static class SubmissionGradingState`.
    - Inheritance: `extends NSMutableDictionary<AssignmentOffering, Map<User, StudentSubmissionInfo>>`.
    - Fields (public):
        - `public NSTimestamp lastFetchTimestamp;`
        - `public NSArray<AssignmentOffering> offerings;`
        - `public NSArray<User> users;`
    - Constructors:
        - **Initial**: `public SubmissionGradingState(NSArray<AssignmentOffering> offerings, NSArray<User> users)`. 
            - Initializes dictionary and sets fields. 
            - `lastFetchTimestamp` should be null or initialized to a base date (e.g., 1970) as appropriate for initial fetch.
        - **Incremental**: `public SubmissionGradingState(SubmissionGradingState existingState)`. 
            - Copies data/state from `existingState`.
    - Helper Methods:
        - `public Map<User, StudentSubmissionInfo> resultsForOffering(AssignmentOffering offering)`: Returns the value for the given offering from the dictionary.
        - `public StudentSubmissionInfo infoForUser(AssignmentOffering ao, User student)`: Returns the info object for a specific student within an offering.
    - Implementation Details:
        - Ensure public getters exist for these fields to follow KVC patterns: `lastFetchTimestamp()`, `offerings()`, `users()`.
  </action>
  <verify>
    - Grep for `SubmissionGradingState` in `Submission.java`.
    - Verify constructors and helper methods.
    - Check for public getters.
  </verify>
  <done>SubmissionGradingState class is implemented with constructors, helpers, and KVC-compatible accessors.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Compilation</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    - Attempt to compile the project to ensure no syntax errors or missing imports.
    - Command: `ant compile` or `make`.
  </action>
  <verify>
    <automated>ant compile</automated>
  </verify>
  <done>Project compiles successfully with new classes.</done>
</task>

</tasks>

<verification>
Check that the new inner classes are present and follow the specified structures. Ensure all fields and methods are accessible and follow KVC patterns.
</verification>

<success_criteria>
1. `Submission.java` compiles without error.
2. `StudentSubmissionInfo` correctly tracks `isDirty` and other required fields.
3. `SubmissionGradingState` provides two constructors (Initial/Incremental) and two helpers (`resultsForOffering`/`infoForUser`).
4. All new fields have KVC-compatible accessors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
