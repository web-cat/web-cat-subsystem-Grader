---
phase: 03-incremental-update-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/org/webcat/grader/Submission.java]
autonomous: false
requirements: [API-02, LOGIC-01]

must_haves:
  truths:
    - "submissionsForGrading only fetches submissions newer than the last fetch timestamp"
    - "state.lastFetchTimestamp is updated to the start time of the query"
    - "User filtering still works via the existing in-memory loop"
  artifacts:
    - path: "src/org/webcat/grader/Submission.java"
      provides: "Incremental fetch logic in submissionsForGrading"
  key_links:
    - from: "state.lastFetchTimestamp()"
      to: "fetchSpec qualifier"
      via: "submitTime.greaterThan"
    - from: "queryStart"
      to: "state.lastFetchTimestamp"
      via: "assignment at method end"
---

<objective>
Implement incremental fetching logic in `Submission.submissionsForGrading(SubmissionGradingState state)`.
This includes capturing the query start time, applying a timestamp-based qualifier, and updating the state with the query start time to ensure no submissions are missed in future calls.
</objective>

<execution_context>
@/Users/edwards/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/edwards/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/org/webcat/grader/Submission.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture query start time and build incremental qualifier</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    Modify `submissionsForGrading(SubmissionGradingState state)` in `src/org/webcat/grader/Submission.java`:
    1. Capture `NSTimestamp queryStart = new NSTimestamp();` at the very beginning of the method.
    2. Build the qualifier:
       - Start with `EOQualifier qual = assignmentOffering.in(offerings);`.
       - If `state.lastFetchTimestamp() != null`, use `ERXQ.and(qual, submitTime.greaterThan(state.lastFetchTimestamp()))` to update the qualifier.
    3. Update the `WCFetchSpecification` to use this qualifier.
    4. Ensure NO database-level user filter is added here (rely on existing in-memory loop).
  </action>
  <verify>
    Check the code to ensure `queryStart` is captured and the qualifier correctly combines `assignmentOffering` and `submitTime` (if applicable) using `ERXQ.and()`.
  </verify>
  <done>Qualifier construction and fetch spec updated in Submission.java with incremental logic</done>
</task>

<task type="auto">
  <name>Task 2: Update lastFetchTimestamp with queryStart</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>
    In `submissionsForGrading(SubmissionGradingState state)` in `src/org/webcat/grader/Submission.java`:
    1. At the end of the method (before the return), update `state.lastFetchTimestamp = queryStart;`.
    2. Ensure this replaces the previous line that set it to a fresh `new NSTimestamp()`.
  </action>
  <verify>
    Verify that `state.lastFetchTimestamp` is set to the `queryStart` captured at the beginning of the method.
  </verify>
  <done>State update logic corrected to use queryStart</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint - Review incremental logic</name>
  <files>src/org/webcat/grader/Submission.java</files>
  <action>Human verification of incremental logic in Submission.java</action>
  <verify>Human review confirmed</verify>
  <done>Incremental update logic approved</done>
  <what-built>Incremental update logic in Submission.java</what-built>
  <how-to-verify>
    1. Review the changes in `src/org/webcat/grader/Submission.java`.
    2. Ensure that the qualifier correctly filters by `submitTime > lastFetchTimestamp`.
    3. Ensure that `queryStart` is used as the next `lastFetchTimestamp`.
    4. Confirm that the in-memory user filtering logic from Phase 2 is preserved and correctly handles user lists.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Review the code changes for correctness against the requirements:
- Use of `queryStart` to avoid gaps.
- Correct combination of qualifiers (AO, Time).
- User filtering still operational via in-memory loop.
- Correct state update.
</verification>

<success_criteria>
- `submissionsForGrading` method correctly implements timestamp-based incremental fetching.
- State is updated with the query start time.
- Qualitative logic avoids database-level user filtering to prevent incorrect results for offerings with different user list specifications.
</success_criteria>

<output>
After completion, create `.planning/phases/03-incremental-update-api/03-01-SUMMARY.md`
</output>
